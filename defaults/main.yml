---

sysctl:
  entries:
    net.ipv6.conf.all.disable_ipv6: 1
    net.ipv6.conf.default.disable_ipv6: 1
    vm.swappiness: 5

base_packages:
  centos:
    - lvm2
    - epel-release
    - python2-pip
  pip:
    - pyOpenSSL
